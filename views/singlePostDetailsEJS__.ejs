<%- include('layout/header') %>

<div
  class="min-h-screen bg-gradient-to-br from-blue-50 to-gray-100 flex items-center justify-center p-8"
>
  <div
    class="max-w-3xl w-full bg-white shadow-2xl rounded-3xl p-10 transform transition duration-300 hover:shadow-3xl"
  >
    <h1
      class="text-xl md:text-2xl font-extrabold text-blue-500 text-center mb-6 tracking-wide"
    >
      <%= userId.title %>
    </h1>

    <!-- Post Metadata -->
    <div
      class="flex justify-between text-gray-500 text-sm lg:text-md border-b pb-5"
    >
      <p class="flex items-center gap-2">
        📅 <span class="font-medium">Created:</span> <%= new
        Date(userId.createdAt).toLocaleString() %>
      </p>
      <p class="flex items-center gap-2">
        ✏️ <span class="font-medium">Updated:</span> <%= new
        Date(userId.updatedAt).toLocaleString() %>
      </p>
    </div>

    <!-- Post Content -->
    <div class="mt-6 text-gray-600 text-md leading-relaxed">
      <p
        class="bg-gray-50 p-7 rounded-2xl shadow-inner border-l-4 border-blue-300"
      >
        <%= userId.body %>
      </p>
    </div>

    <!-- Back Button -->
    <div class="mt-10 text-center">
      <a
        href="/"
        class="inline-block px-5 py-3 bg-blue-500 text-white font-semibold rounded-2xl shadow-md transition-all transform hover:scale-105 hover:bg-blue-600 hover:shadow-lg"
      >
        ⬅️ Back to Home
      </a>
    </div>
  </div>
</div>
