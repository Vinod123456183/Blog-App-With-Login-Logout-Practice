<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="">
    <%- include('layout/dashBoard_header') %>

    <main
      class="flex-grow px-8 py-6 flex justify-center items-center min-h-screen"
    >
      <div
        class="w-full max-w-2xl bg-white p-8 rounded-2xl shadow-xl border border-gray-200"
      >
        <h3 class="text-3xl font-bold text-center text-gray-800 mb-6">
          ✏️ Edit Post
        </h3>

        <form
          action="/admin/submit-post/<%= data._id %>"
          method="POST"
          class="space-y-6"
        >
          <div>
            <label for="title" class="block text-gray-700 font-medium mb-2"
              >Title</label
            >
            <input
              type="text"
              id="title"
              name="title"
              value="<%= data.title %>"
              required
              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
              placeholder="Enter post title"
            />
          </div>
          <div>
            <label for="body" class="block text-gray-700 font-medium mb-2"
              >Body</label
            >
            <textarea
              id="body"
              name="body"
              required
              rows="5"
              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
              placeholder="Enter post content"
            >
<%= data.body %></textarea
            >
          </div>
          <div class="text-center">
            <button
              type="submit"
              class="px-6 py-3 bg-blue-600 text-white font-semibold rounded-lg shadow-md hover:bg-blue-700 transition duration-200"
            >
              Update
            </button>
          </div>
        </form>
      </div>
    </main>
  </body>
</html>
